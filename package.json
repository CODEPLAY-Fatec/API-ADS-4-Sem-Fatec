{
    "scripts": {
        "start:dev": "concurrently \"cd backend && npm install && npm run dev\" \"cd frontend && npm install && npm run dev\"",
        "start:prod": "concurrently \"cd backend && npm install && npm run build && npm start\" \"cd frontend && npm install && npm run build && npm start\""
    }
}
